// import java.util.*;
public class SortingTechniques{
    //DISPLAY ARRAY
    public static void displayArray(int arr[])
    {
        for(int i=0;i<arr.length;i++)
        {
            System.out.print(arr[i]+" ");
        }
    }

    //BUBBLE SORT
    public static void bubbleSort(int arr[])
    {
        for(int i=0;i<arr.length-1;i++)
        {
            for(int j=0;j<arr.length-1-i;j++)
            {
                if(arr[j]>arr[j+1])
                {
                    int temp=arr[j];
                    arr[j]=arr[j+1];
                    arr[j+1]=temp;
                }
            }
        }
    }

    //SELECTION SORT
    public static void selectionSort(int arr[])
    {
        for(int i=0;i<arr.length-1;i++)
        {
            int minPos=i;
            for(int j=i+1;j<arr.length;j++)
            {
                if(arr[minPos]>arr[j])
                {
                    minPos=j;
                }
            }
            int temp=arr[minPos];
            arr[minPos]=arr[i];
            arr[i]=temp;
        }
    }

    //INSERTION SORT
    public static void insertionSort(int arr[])
    {
        for(int i=1;i<arr.length;i++)
        {
            int curr=arr[i];
            int prev=i-1;
            while(prev>=0 && arr[prev]>curr)
            {
                arr[prev+1]=arr[prev];
                prev--;
            }
            arr[prev+1]=curr;
        }
    }

    //MERGE FUNCTION FOR MERGE SORT
    public static void merge(int arr[],int startIndex,int mid,int endIndex)
    {
        int[] temp=new int[endIndex-startIndex+1];
        int i=startIndex;//left part iterator
        int j=mid+1;//right part iterator
        int k=0;//temp array iterrator
        while(i<=mid&& j<=endIndex)
        {
            if(arr[i]<arr[j])
            {
                temp[k]=arr[i];
                i++;
            }
            else{
                temp[k]=arr[j];
                j++;
            }
            k++;
        }
        while(i<=mid)
        {   
            temp[k++]=arr[i++];
           
        }
        while(j<=endIndex)
        {   
            temp[k++]=arr[j++];
            
        }
        for(i=startIndex,k=0;i<=endIndex;i++,k++)
        {
            arr[i]=temp[k];
        }
    }

    //MERGE SORT
    public static void mergeSort(int arr[],int startIndex,int endIndex)
    {   if(startIndex>=endIndex) return;
        int mid=startIndex+(endIndex-startIndex)/2;
        mergeSort(arr,startIndex,mid);
        mergeSort(arr,mid+1,endIndex);
        merge(arr,startIndex,mid,endIndex);

    }

    //QUICK SORT
    public static void quickSort(int arr[],int start,int end)
    {
        if(start >=end) return;
        int pivot=partition(arr,start,end);
        quickSort(arr,start,pivot-1);
        quickSort(arr,pivot+1,end);
        
    }

    //Partition function for quick sort
    public static int partition(int arr[],int start,int end)
    {
        int pivot=arr[end];
        int i=start-1;
        for(int j=start;j<=end;j++)
        {
            if(arr[j]<pivot)  //if = is not used then again need to use the swap logic
            {   i++;
                int temp=arr[j];
                arr[j]=arr[i];
                arr[i]=temp;
            }
        } 
        int temp=pivot;
        arr[end]=arr[i+1];
        arr[i+1]=temp;
         return i;
    }
    public static void main(String[] args)
    {
        int array[]={5,8,3,9,7,6,4,1,2};
        
        // bubbleSort(array);
        // selectionSort(array);
        // insertionSort(array);
        // displayArray(array);
        //mergeSort(array,0,array.length-1);
      quickSort(array,0,array.length-1);
        displayArray(array);
    
    }
}
